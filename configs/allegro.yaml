runtime:
  device: cuda
  batch_size: 2
  num_workers: 0

model:
  name: allegro
  checkpoint_path: models/allegro/vae
  layer_specs:
    - down_blocks.0
    - mid_block
    - conv_out

output:
  save_tokens: true
  save_pooled: true
