runtime:
  device: cpu
  batch_size: 1
  num_workers: 0

dataset:
  name: dataset_v0
  metadata_file: metadata.example.jsonl
  split: train

model:
  name: videocrafter
  checkpoint_path: null
  layer_specs:
    - down_block_00
    - mid_block
    - up_block_00

output:
  save_tokens: true
  save_pooled: true
